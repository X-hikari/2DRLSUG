public enum BuffEffectType {
    Numeric,
    Status,
    Trigger
}

public enum PlayerAttribute {
    Attack,
    MoveSpeed,
    MaxHealth,
    // 其他属性...
}

public enum PlayerStatus {
    Invisible,
    Invincible,
    // 其他状态...
}
